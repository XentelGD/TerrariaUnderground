using UnityEngine;
using Environment;

public class GameManager : MonoBehaviour
{

    private World World = new World();
    
    void Start()
    {
        
    }

    // Update is called once per frame
    void Update()
    {
        
    }
}
