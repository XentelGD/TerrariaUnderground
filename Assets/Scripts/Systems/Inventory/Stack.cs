namespace Data
{
    /* ------------------------------------------------------------------
     * Класс, который позволяет создавать "комплект предметов".
     * Например, есть предмет "Палка", но нам нужно ещё хранить количество
     * этих палок. Для этого и нужен Stack.
     * ------------------------------------------------------------------
     */
    
    public class Stack
    {
        public string Item;
        public int Amount;

        public Stack(string item, int amount)
        {
            Item = item;
            Amount = amount;
        }
    }
}